#ifndef EXECUTOR_H
#define EXECUTOR_H

#include <vector>
#include <string>

void execute_command(std::vector<std::string> &args);

#endif

#ifndef EXECUTOR_H
#define EXECUTOR_H

#include <vector>
#include <string>

void execute_command(std::vector<std::string> &args);

#endif